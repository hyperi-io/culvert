culvert has been deployed.

1. Get the metrics/health endpoint:
   kubectl port-forward svc/{{ include "culvert.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
   curl http://localhost:{{ .Values.service.port }}/livez
   curl http://localhost:{{ .Values.service.port }}/metrics

{{- if .Values.keda.enabled }}

2. Check KEDA autoscaling status:
   kubectl get scaledobject {{ include "culvert.fullname" . }}
   kubectl get hpa
{{- end }}

3. View logs:
   kubectl logs -l app.kubernetes.io/name={{ include "culvert.name" . }} -f
