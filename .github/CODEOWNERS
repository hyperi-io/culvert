# Culvert code ownership. Ownership sits with HyperI; GitHub requires a
# resolvable user or team handle here, so swap in the org team
# (e.g. @hyperi-io/<team>) once one is designated for this repo.
* @catinspace-au
