## Summary

<!-- 1-3 bullets describing what this PR changes and why. -->

## Test plan

- [ ] Unit tests pass locally (`pytest tests/unit/ -q`)
- [ ] `docker build -t hyperi-vpn:test .` succeeds
- [ ] E2E tests pass (if Docker + test VM available)

## Checklist

- [ ] Commit messages follow conventional-commits (`fix:`, `feat:`, `docs:`, etc.)
- [ ] Docs updated if user-facing behaviour or configuration changes
- [ ] No internal URLs, hostnames, or HyperI-specific defaults introduced
